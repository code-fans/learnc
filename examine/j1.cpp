#include <iostream>
#include <string>
using namespace std;
int cin1()
{
    char a;
    cin>>a;
    if(a=='(')
        return 1;
    if(a==')')
        return 2;
    return 0;
}
int main()
{
    string str;
    while(getline(cin, str);)
        int a[10000];
        for (int i = 0; i < str.sise(); i++)
        {
            /* code */
        }
    
}