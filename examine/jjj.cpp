#include <iostream>
#include <string>

#include <cctype>

using namespace std;
int main()
{
    string str,ans,ans1;
    while (cin>>str){
        if (ans.size()>str.size()||ans.size()==0){
            ans=str;
        }
        if (ans1.size()>str.size()||ans1.size()==0){
            ans1=str;
        }
    }
    cout<<ans<<endl<<ans1<<endl;
    return 0;
}